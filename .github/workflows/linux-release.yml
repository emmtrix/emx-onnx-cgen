name: linux-release

on:
  release:
    types: [published]

jobs:
  build-linux-bundle:
    runs-on: ubuntu-latest
    container:
      image: quay.io/pypa/manylinux_2_28_x86_64
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        run: |
          python_bin="/opt/python/cp311-cp311/bin/python"
          echo "PYTHON_BIN=${python_bin}" >> "$GITHUB_ENV"
          "${python_bin}" --version

      - name: Install dependencies
        run: |
          "${PYTHON_BIN}" -m pip install --upgrade pip
          "${PYTHON_BIN}" -m pip install -r requirements-ci.txt
          "${PYTHON_BIN}" -m pip install pyinstaller

      - name: Build PyInstaller bundle
        run: |
          "${PYTHON_BIN}" -m PyInstaller --clean --noconfirm --onedir --name emx-onnx-cgen --paths src src/emx_onnx_cgen/cli.py

      - name: Package bundle
        run: |
          tar -czf emx-onnx-cgen-linux-amd64.tar.gz -C dist/emx-onnx-cgen .

      - name: Upload release asset
        uses: softprops/action-gh-release@v2
        with:
          files: emx-onnx-cgen-linux-amd64.tar.gz
