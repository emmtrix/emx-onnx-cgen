{% if rng_requires_u64 %}
static uint64_t rng_state = 0x243f6a8885a308d3ull;

static uint64_t rng_next_u64(void) {
    uint64_t x = rng_state;
    x ^= x >> 12;
    x ^= x << 25;
    x ^= x >> 27;
    rng_state = x;
    return x * 0x2545f4914f6cdd1dull;
}
{% endif %}

{% if rng_requires_float %}
static float rng_next_float(void) {
    return (float)((double)rng_next_u64() * (1.0 / 18446744073709551616.0));
}
{% endif %}

{% if rng_requires_double %}
static double rng_next_double(void) {
    return (double)rng_next_u64() * (1.0 / 18446744073709551616.0);
}
{% endif %}

{% if rng_requires_i64 %}
static int64_t rng_next_i64(void) {
    return (int64_t)rng_next_u64();
}
{% endif %}

{% for input in inputs %}
{% if input.constant_lines %}
{% if input.is_string %}
static const {{ input.c_type }} {{ input.constant_name }}{{ input.array_suffix }} = {
{% else %}
static const {{ input.c_type }} {{ input.constant_name }}[] = {
{% endif %}
{% for line in input.constant_lines %}
{{ line }}
{% endfor %}
};
{% endif %}
{% endfor %}

__attribute__((weak, noinline)) void timer_start(void) {}
__attribute__((weak, noinline)) void timer_stop(void) {}

{% set constant_inputs = inputs | selectattr('constant_name') | list %}
{% set variable_inputs = inputs | rejectattr('constant_name') | list %}

static void testbench_init_constant_input({% if not constant_inputs %}void{% else %}{% for input in constant_inputs %}{{ input.c_type }} {{ input.name }}{{ input.array_suffix }}{% if not loop.last %}, {% endif %}{% endfor %}{% endif %}) {
{% if not constant_inputs %}
    (void)0;
{% endif %}
{% for input in inputs %}
{% if input.constant_name %}
{% if input.rank == 0 %}
{% if input.is_string %}
    memcpy({{ input.name }}, {{ input.constant_name }}, EMX_STRING_MAX_LEN * sizeof(char));
{% else %}
    {{ input.name }} = {{ input.constant_name }}[0];
{% endif %}
{% else %}
{% for depth in range(input.rank) %}
    for (idx_t {{ input.loop_vars[depth] }} = 0; {{ input.loop_vars[depth] }} < {{ input.shape[depth] }}; ++{{ input.loop_vars[depth] }}) {
{% endfor %}
{% if input.is_string %}
        memcpy({{ input.name }}{{ input.array_index_expr }}, {{ input.constant_name }}{{ input.array_index_expr }}, EMX_STRING_MAX_LEN * sizeof(char));
{% else %}
        {{ input.name }}{{ input.array_index_expr }} = {{ input.constant_name }}[{{ input.index_expr }}];
{% endif %}
{% for depth in range(input.rank - 1, -1, -1) %}
    }
{% endfor %}
{% endif %}
{% endif %}
{% endfor %}
}

static int testbench_read_input_file(
    const char *input_path{% for dim in dim_args %}, int *{{ dim.name }}__ptr{% endfor %}{% for input in variable_inputs %}, {{ input.c_type }} {{ input.name }}{{ input.array_suffix }}{% if input.is_sequence %}, idx_t *{{ input.count_name }}{% for dim in input.sequence_dim_arrays %}, idx_t {{ dim.name }}[EMX_SEQUENCE_MAX_LEN]{% endfor %}{% endif %}{% if input.optional_flag_name %}, _Bool *{{ input.optional_flag_name }}{% endif %}{% endfor %}
) {
{% if not variable_inputs %}
    (void)input_path;
{% for dim in dim_args %}
    (void){{ dim.name }}__ptr;
{% endfor %}
    return 0;
{% else %}
    FILE *input_file = fopen(input_path, "rb");
    if (!input_file) {
        fprintf(stderr, "Failed to open input file: %s\n", input_path);
        return 1;
    }
{% for input in inputs %}
{% if not input.constant_name %}
{% if input.optional_flag_name %}
    {
        uint8_t present = 1u;
        if (fread(&present, sizeof(uint8_t), 1, input_file) != 1) {
            fprintf(stderr, "Failed to read optional presence for input {{ input.json_name }}\n");
            fclose(input_file);
            return 1;
        }
        *{{ input.optional_flag_name }} = (present != 0u);
    }
{% endif %}
{% if input.is_sequence %}
    {
        int32_t sequence_count = 0;
        if (fread(&sequence_count, sizeof(int32_t), 1, input_file) != 1) {
            fprintf(stderr, "Failed to read sequence count for input {{ input.json_name }}\n");
            fclose(input_file);
            return 1;
        }
        if (sequence_count < 0 || sequence_count > EMX_SEQUENCE_MAX_LEN) {
            fprintf(stderr, "Invalid sequence count for input {{ input.json_name }}: %d\n", (int)sequence_count);
            fclose(input_file);
            return 1;
        }
        *{{ input.count_name }} = (idx_t)sequence_count;
{% for dim in input.sequence_dim_arrays %}
        for (idx_t seq = 0; seq < EMX_SEQUENCE_MAX_LEN; ++seq) {
            {{ dim.name }}[seq] = 0;
        }
        if (sequence_count > 0) {
            if (fread({{ dim.name }}, sizeof(int32_t), (size_t)sequence_count, input_file) != (size_t)sequence_count) {
                fprintf(stderr, "Failed to read sequence dim axis {{ dim.axis }} for input {{ input.json_name }}\n");
                fclose(input_file);
                return 1;
            }
        }
{% endfor %}
    }
{% endif %}
{% if input.optional_flag_name and not input.is_sequence %}
    if (*{{ input.optional_flag_name }}) {
{% endif %}
{% for dim_read in input.runtime_dim_reads %}
    {
        int32_t runtime_dim = 0;
        if (fread(&runtime_dim, sizeof(int32_t), 1, input_file) != 1) {
            fprintf(stderr, "Failed to read runtime dim {{ dim_read.name }} for input {{ input.json_name }}\n");
            fclose(input_file);
            return 1;
        }
        if (runtime_dim < 0) {
            fprintf(stderr, "Invalid runtime dim {{ dim_read.name }} for input {{ input.json_name }}: %d\n", (int)runtime_dim);
            fclose(input_file);
            return 1;
        }
        *{{ dim_read.name }}__ptr = runtime_dim;
    }
{% endfor %}
{% for depth in range(input.rank) %}
    for (idx_t {{ input.loop_vars[depth] }} = 0; {{ input.loop_vars[depth] }} < {{ input.input_file_shape[depth] }}; ++{{ input.loop_vars[depth] }}) {
{% endfor %}
{% if input.is_string %}
        if (fread({{ input.name }}{{ input.array_index_expr }}, sizeof(char), EMX_STRING_MAX_LEN, input_file) != EMX_STRING_MAX_LEN) {
{% else %}
        if (fread(&{{ input.name }}{{ input.array_index_expr }}, sizeof({{ input.c_type }}), 1, input_file) != 1) {
{% endif %}
            fprintf(stderr, "Failed to read input {{ input.json_name }}\n");
            fclose(input_file);
            return 1;
        }
{% for depth in range(input.rank - 1, -1, -1) %}
    }
{% endfor %}
{% if input.optional_flag_name and not input.is_sequence %}
    }
{% endif %}
{% endif %}
{% endfor %}

    fclose(input_file);
    return 0;
{% endif %}
}

static void testbench_fill_random_input({% if not variable_inputs and not dim_args %}void{% else %}{% for dim in dim_args %}int {{ dim.name }}{% if not loop.last or variable_inputs %}, {% endif %}{% endfor %}{% for input in variable_inputs %}{{ input.c_type }} {{ input.name }}{{ input.array_suffix }}{% if input.is_sequence %}, idx_t {{ input.count_name }}{% for dim in input.sequence_dim_arrays %}, idx_t {{ dim.name }}[EMX_SEQUENCE_MAX_LEN]{% endfor %}{% endif %}{% if not loop.last %}, {% endif %}{% endfor %}{% endif %}) {
{% if not variable_inputs and not dim_args %}
    (void)0;
{% endif %}
{% for dim in dim_args %}
    (void){{ dim.name }};
{% endfor %}
{% for input in inputs %}
{% if not input.constant_name %}
{% if input.is_sequence %}
{% for dim in input.sequence_dim_arrays %}
    for (idx_t seq = 0; seq < {{ input.count_name }}; ++seq) {
        {{ dim.name }}[seq] = {{ dim.default_value }};
    }
{% endfor %}
{% endif %}
{% for depth in range(input.rank) %}
    for (idx_t {{ input.loop_vars[depth] }} = 0; {{ input.loop_vars[depth] }} < {{ input.runtime_shape[depth] }}; ++{{ input.loop_vars[depth] }}) {
{% endfor %}
{% if input.is_string %}
        {{ input.name }}{{ input.array_index_expr }}[0] = '\0';
{% else %}
        {{ input.name }}{{ input.array_index_expr }} = {{ input.random_expr }};
{% endif %}
{% for depth in range(input.rank - 1, -1, -1) %}
    }
{% endfor %}
{% endif %}
{% endfor %}
}

static void testbench_print_json(
{% for output in outputs %}
    {{ output.c_type }} {{ output.name }}{{ output.array_suffix }}{% if output.is_sequence %}, idx_t {{ output.count_name }}{% for dim in output.sequence_dim_arrays %}, idx_t {{ dim.name }}[EMX_SEQUENCE_MAX_LEN]{% endfor %}{% endif %}{% if not loop.last %},
{% endif %}
{% endfor %}
) {
    printf("{\n  \"outputs\": {\n");
{% for output in outputs %}
{% if not loop.first %}
    printf(",\n");
{% endif %}
{% if output.is_sequence %}
    printf("    \"{{ output.json_name }}\": {\"shape\": [{{ output.shape_literal }}], \"sequence_count\": %d, \"item_shapes\": [", (int){{ output.count_name }});
    for (idx_t seq = 0; seq < {{ output.count_name }}; ++seq) {
        if (seq) {
            printf(",");
        }
        printf("[");
{% for dim in output.sequence_dim_arrays %}
        if ({{ loop.index0 }}) {
            printf(",");
        }
        printf("%d", (int){{ dim.name }}[seq]);
{% endfor %}
{% if not output.sequence_dim_arrays %}
{% for axis in range(output.rank - 1) %}
        if ({{ axis }}) {
            printf(",");
        }
        printf("%d", {{ output.shape[axis + 1] }});
{% endfor %}
{% endif %}
        printf("]");
    }
    printf("], \"data\":\n      ");
{% else %}
    printf("    \"{{ output.json_name }}\": {\"shape\": [{{ output.shape_literal }}], \"data\":\n      ");
{% endif %}
    printf("[");
{% for depth in range(output.rank) %}
for (idx_t {{ output.loop_vars[depth] }} = 0; {{ output.loop_vars[depth] }} < {{ output.shape[depth] }}; ++{{ output.loop_vars[depth] }}) {
    if ({{ output.loop_vars[depth] }}) {
{% if depth == 0 %}
        printf(",\n      ");
{% else %}
        printf(",");
{% endif %}
    }
{% if depth < output.rank - 1 %}
    printf("[");
{% endif %}
{% endfor %}
{% if output.is_string %}
printf("\"%s\"", {{ output.name }}{{ output.array_index_expr }});
{% else %}
printf("{{ output.print_format }}", {{ output.print_cast }}{{ output.name }}{{ output.array_index_expr }}{{ output.print_suffix }});
{% endif %}
{% for depth in range(output.rank - 1, -1, -1) %}
{% if depth < output.rank - 1 %}
    printf("]");
{% endif %}
}
{% endfor %}
    printf("]}");
{% endfor %}
    printf("\n  }\n}\n");
}

{% if testbench_output_format == "txt" %}
static void testbench_print_txt(
{% for output in outputs %}
    {{ output.c_type }} {{ output.name }}{{ output.array_suffix }}{% if output.is_sequence %}, idx_t {{ output.count_name }}{% for dim in output.sequence_dim_arrays %}, idx_t {{ dim.name }}[EMX_SEQUENCE_MAX_LEN]{% endfor %}{% endif %}{% if not loop.last %},
{% endif %}
{% endfor %}
) {
{% for output in outputs %}
{% if output.is_sequence %}
    printf("{{ output.json_name }} (sequence_count=%d):\n", (int){{ output.count_name }});
{% else %}
    printf("{{ output.json_name }}:\n");
{% endif %}
{% for depth in range(output.rank) %}
for (idx_t {{ output.loop_vars[depth] }} = 0; {{ output.loop_vars[depth] }} < {{ output.shape[depth] }}; ++{{ output.loop_vars[depth] }}) {
    if ({{ output.loop_vars[depth] }}) {
{% if depth == 0 %}
        printf("\n");
{% else %}
        printf(" ");
{% endif %}
    }
{% if depth < output.rank - 1 %}
    printf("[");
{% endif %}
{% endfor %}
{% if output.is_string %}
printf("%s", {{ output.name }}{{ output.array_index_expr }});
{% else %}
printf("{{ output.print_format }}", {{ output.print_cast }}{{ output.name }}{{ output.array_index_expr }}{{ output.print_suffix }});
{% endif %}
{% for depth in range(output.rank - 1, -1, -1) %}
{% if depth < output.rank - 1 %}
    printf("]");
{% endif %}
}
{% endfor %}
    printf("\n");
{% if not loop.last %}
    printf("\n");
{% endif %}
{% endfor %}
}

{% endif %}
{% if testbench_output_format == "txt-emmtrix" %}
static void testbench_print_txt_emmtrix(
{% for output in outputs %}
    {{ output.c_type }} {{ output.name }}{{ output.array_suffix }}{% if output.is_sequence %}, idx_t {{ output.count_name }}{% for dim in output.sequence_dim_arrays %}, idx_t {{ dim.name }}[EMX_SEQUENCE_MAX_LEN]{% endfor %}{% endif %}{% if not loop.last %},
{% endif %}
{% endfor %}
) {
{% for output in outputs %}
{% for depth in range(output.rank) %}
for (idx_t {{ output.loop_vars[depth] }} = 0; {{ output.loop_vars[depth] }} < {{ output.shape[depth] }}; ++{{ output.loop_vars[depth] }}) {
{% endfor %}
{% if output.is_string %}
    printf("#ULP{{ testbench_emmtrix_ulp_tag }} %s[%zu] = %s\n", "{{ output.json_name }}", (size_t){{ output.index_expr }}, {{ output.name }}{{ output.array_index_expr }});
{% else %}
    printf("#ULP{{ testbench_emmtrix_ulp_tag }} %s[%zu] = %a\n", "{{ output.json_name }}", (size_t){{ output.index_expr }}, (double){{ output.name }}{{ output.array_index_expr }});
{% endif %}
{% for depth in range(output.rank - 1, -1, -1) %}
}
{% endfor %}
{% endfor %}
}

{% endif %}
static int testbench_run(const char *input_path) {
{% for dim in dim_args %}
    int {{ dim.name }} = {{ dim.value }};
{% endfor %}

{% for input in inputs %}
{% if input.optional_flag_name %}
    _Bool {{ input.optional_flag_name }} = {{ "1" if input.optional_present else "0" }};
{% endif %}
{% if input.is_sequence %}
    {{ input.storage }}{{ input.c_type }} {{ input.name }}{{ input.array_suffix }};
    idx_t {{ input.count_name }} = {{ input.count_value }};
{% for dim in input.sequence_dim_arrays %}
    idx_t {{ dim.name }}[EMX_SEQUENCE_MAX_LEN] = {0};
{% endfor %}
{% else %}
    {{ input.storage }}{{ input.c_type }} {{ input.name }}{{ input.array_suffix }};
{% endif %}
{% endfor %}

    testbench_init_constant_input({% for input in constant_inputs %}{{ input.name }}{% if not loop.last %}, {% endif %}{% endfor %});
    if (input_path) {
        if (testbench_read_input_file(input_path{% for dim in dim_args %}, &{{ dim.name }}{% endfor %}{% for input in variable_inputs %}, {{ input.name }}{% if input.is_sequence %}, &{{ input.count_name }}{% for dim in input.sequence_dim_arrays %}, {{ dim.name }}{% endfor %}{% endif %}{% if input.optional_flag_name %}, &{{ input.optional_flag_name }}{% endif %}{% endfor %}) != 0) {
            return 1;
        }
    } else {
        testbench_fill_random_input({% for dim in dim_args %}{{ dim.name }}{% if not loop.last or variable_inputs %}, {% endif %}{% endfor %}{% for input in variable_inputs %}{{ input.name }}{% if input.is_sequence %}, {{ input.count_name }}{% for dim in input.sequence_dim_arrays %}, {{ dim.name }}{% endfor %}{% endif %}{% if not loop.last %}, {% endif %}{% endfor %});
    }

{% for output in outputs %}
{% if output.is_sequence %}
    {{ output.storage }}{{ output.c_type }} {{ output.name }}{{ output.array_suffix }};
    idx_t {{ output.count_name }} = 0;
{% for dim in output.sequence_dim_arrays %}
    idx_t {{ dim.name }}[EMX_SEQUENCE_MAX_LEN] = {0};
{% endfor %}
{% else %}
    {{ output.storage }}{{ output.c_type }} {{ output.name }}{{ output.array_suffix }};
{% endif %}
{% if output.optional_flag_name %}
    _Bool {{ output.optional_flag_name }} = 0;
{% endif %}
{% endfor %}

    if (!{{ model_name }}_load("{{ weight_data_filename }}")) {
        return 1;
    }

    timer_start();
    {{ model_name }}({% for dim in dim_args %}{{ dim.name }}, {% endfor %}{% for input in inputs %}{{ input.name }}{% if input.is_sequence %}, {{ input.count_name }}{% for dim in input.sequence_dim_arrays %}, {{ dim.name }}{% endfor %}{% endif %}{% if input.optional_flag_name %}, {{ input.optional_flag_name }}{% endif %}, {% endfor %}{% for output in outputs %}{{ output.name }}{% if output.is_sequence %}, &{{ output.count_name }}{% for dim in output.sequence_dim_arrays %}, {{ dim.name }}{% endfor %}{% endif %}{% if output.optional_flag_name %}, &{{ output.optional_flag_name }}{% endif %}{% if not loop.last %}, {% endif %}{% endfor %});
    timer_stop();

{% if testbench_output_format == "txt" %}
    testbench_print_txt({% for output in outputs %}{{ output.name }}{% if output.is_sequence %}, {{ output.count_name }}{% for dim in output.sequence_dim_arrays %}, {{ dim.name }}{% endfor %}{% endif %}{% if not loop.last %}, {% endif %}{% endfor %});
{% elif testbench_output_format == "txt-emmtrix" %}
    testbench_print_txt_emmtrix({% for output in outputs %}{{ output.name }}{% if output.is_sequence %}, {{ output.count_name }}{% for dim in output.sequence_dim_arrays %}, {{ dim.name }}{% endfor %}{% endif %}{% if not loop.last %}, {% endif %}{% endfor %});
{% else %}
    testbench_print_json({% for output in outputs %}{{ output.name }}{% if output.is_sequence %}, {{ output.count_name }}{% for dim in output.sequence_dim_arrays %}, {{ dim.name }}{% endfor %}{% endif %}{% if not loop.last %}, {% endif %}{% endfor %});
{% endif %}
    return 0;
}

int main(int argc, char **argv) {
    const char *input_path = NULL;

    if (argc > 1) {
        input_path = argv[1];
    }

    return testbench_run(input_path);
}
