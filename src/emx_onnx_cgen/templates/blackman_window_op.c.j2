EMX_NODE_FN void {{ op_name }}({{ dim_args }}{{ params | join(', ') }}) {
    const {{ compute_type }} size_value = ({{ compute_type }}){{ size }}[0];
    const {{ compute_type }} denom = {{ periodic_literal }} ? size_value : (size_value - {{ one_literal }});
    const {{ compute_type }} pi = {{ pi_literal }};
    for (idx_t idx = 0; idx < {{ length }}; ++idx) {
        const {{ compute_type }} phase = ({{ two_literal }} * pi * ({{ compute_type }})idx) / denom;
        const {{ compute_type }} value = {{ c042_literal }} - {{ c05_literal }} * {{ scalar_fn(SF.COS, compute_scalar_dtype) }}(phase) + {{ c008_literal }} * {{ scalar_fn(SF.COS, compute_scalar_dtype) }}({{ two_literal }} * phase);
        {{ output }}[idx] = ({{ c_type }})value;
    }
}
