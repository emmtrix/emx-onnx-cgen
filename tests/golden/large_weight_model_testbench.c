/*
 * Generated by emmtrix ONNX-to-C Code Generator (emx-onnx-cgen)
 *
 * Codegen settings:
 *   emit_testbench: True
 *   restrict_arrays: True
 *   fp32_accumulation_strategy: simple
 *   fp16_accumulation_strategy: fp32
 *   large_temp_threshold: 1024
 *   large_weight_threshold: 4
 * Model checksum (sha256): 62592cc3778a960ae261e3348b338db2e9ec99d4eb485f7461a6b94fc6df5e3e
 * Model name: large_weight_model
 * Graph name: add_large_init_graph
 * Inputs: 1 Outputs: 1 Nodes: 1 Initializers: 1
 * IR version: 7
 * Model version: n/a
 * Domain: n/a
 * Producer: emx-onnx-cgen (version: n/a)
 * Opset imports: ai.onnx=13
 * Description:
 *   n/a
 * Graph description:
 *   n/a
 * Metadata:
 *   n/a
 */

#include <stdint.h>
#include <stdio.h>
#include <math.h>
#include <float.h>

#ifndef idx_t
#define idx_t int32_t
#endif
#ifndef EMX_UNUSED
#if defined(__GNUC__) || defined(__clang__)
#define EMX_UNUSED __attribute__((unused))
#else
#define EMX_UNUSED
#endif
#endif

extern float weight1_weight_large[2][3];

/*
 * Weight 1:
 * Name: weight1_weight_large
 * Shape: (2, 3)
 * Elements: 6
 * Dtype: float
 */
float weight1_weight_large[2][3];

static inline float ref_scalar_f32_add(float a, float b) {
    return a + b;
}

/*
 * Node 0:
 * OpType: Add
 * Name: n/a
 * Inputs: in0, weight_large
 * Outputs: out
 * Attrs: n/a
 */
static inline void node0_add(const float input0[2][3], const float input1[2][3], float output[2][3]) {
    for (idx_t i0 = 0; i0 < 2; ++i0) {
        for (idx_t i1 = 0; i1 < 3; ++i1) {
            output[i0][i1] = ref_scalar_f32_add(input0[i0][i1], input1[i0][i1]);
        }
    }
}

static _Bool large_weight_model_load_file(FILE *file);

_Bool large_weight_model_load(const char *path) {
    FILE *file = fopen(path, "rb");
    if (!file) {
        return 0;
    }
    _Bool ok = large_weight_model_load_file(file);
    fclose(file);
    return ok;
}

static _Bool large_weight_model_load_file(FILE *file) {
    for (idx_t i0 = 0; i0 < 2; ++i0) {
        for (idx_t i1 = 0; i1 < 3; ++i1) {
            if (fread(&weight1_weight_large[i0][i1], sizeof(weight1_weight_large[0][0]), 1, file) != 1) {
                return 0;
            }
        }
    }
    return 1;
}

void large_weight_model(const float in0[restrict 2][3], float out[restrict 2][3]) {
    node0_add(in0, weight1_weight_large, out);
}

static uint64_t rng_state = 0x243f6a8885a308d3ull;

static uint64_t rng_next_u64(void) {
    uint64_t x = rng_state;
    x ^= x >> 12;
    x ^= x << 25;
    x ^= x >> 27;
    rng_state = x;
    return x * 0x2545f4914f6cdd1dull;
}

static float rng_next_float(void) {
    return (float)((double)rng_next_u64() * (1.0 / 18446744073709551616.0));
}





int main(int argc, char **argv) {
    FILE *input_file = NULL;
    if (argc > 1) {
        input_file = fopen(argv[1], "rb");
        if (!input_file) {
            fprintf(stderr, "Failed to open input file: %s\n", argv[1]);
            return 1;
        }
    }

    float in0[2][3];
    if (input_file) {
        for (idx_t i0 = 0; i0 < 2; ++i0) {
            for (idx_t i1 = 0; i1 < 3; ++i1) {
                if (fread(&in0[i0][i1], sizeof(float), 1, input_file) != 1) {
                    fprintf(stderr, "Failed to read input in0\n");
                    return 1;
                }
            }
        }
    } else {
        for (idx_t i0 = 0; i0 < 2; ++i0) {
            for (idx_t i1 = 0; i1 < 3; ++i1) {
                in0[i0][i1] = rng_next_float();
            }
        }
    }
    if (input_file) {
        fclose(input_file);
    }

    float out[2][3];

    if (!large_weight_model_load("large_weight_model.bin")) {
        return 1;
    }
    large_weight_model(in0, out);

    printf("{\"inputs\":{");
    printf("\"in0\":{\"shape\":[2,3],\"data\":");
    printf("[");
    for (idx_t i0 = 0; i0 < 2; ++i0) {
        if (i0) {
            printf(",");
        }
        printf("[");
        for (idx_t i1 = 0; i1 < 3; ++i1) {
            if (i1) {
                printf(",");
            }
            printf("\"%a\"", (double)in0[i0][i1]);
        }
        printf("]");
    }
    printf("]}");

    printf("},\"outputs\":{");
    printf("\"out\":{\"shape\":[2,3],\"data\":");
    printf("[");
    for (idx_t i0 = 0; i0 < 2; ++i0) {
        if (i0) {
            printf(",");
        }
        printf("[");
        for (idx_t i1 = 0; i1 < 3; ++i1) {
            if (i1) {
                printf(",");
            }
            printf("\"%a\"", (double)out[i0][i1]);
        }
        printf("]");
    }
    printf("]}");
    printf("}}\n");
    return 0;
}
